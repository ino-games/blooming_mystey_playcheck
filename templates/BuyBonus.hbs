<!-- BuyBonus -->
{{> GameMode "buyBonus" }}
<div>
    <table>
        <tr>
            <td class="bold">
                <**'bet'**>
            </td>
            <td>{{decimal bet}}</td>
        </tr>
        <tr>
            <td class="bold" style="padding-right: 10px;">
                <**'cost'**> ({{decimal costMultiplier}} * <**'bet'**>)
            </td>
            <td>{{decimal cost}}</td>
        </tr>
    </table>
</div>